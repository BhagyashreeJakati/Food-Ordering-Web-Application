spring.application.name=FoodOrderingService

# Database — set DB_URL, DB_USERNAME, DB_PASSWORD as environment variables
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3310/food_ordering_system?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Bhagyashree@02}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Razorpay — set RAZORPAY_KEY and RAZORPAY_SECRET as environment variables
razorpay.api.key=${RAZORPAY_KEY:rzp_test_SdqxBx3Npbtz1O}
razorpay.api.secret=${RAZORPAY_SECRET:***********************}

logging.level.org.springframework.security=WARN

# Email — set MAIL_USERNAME and MAIL_PASSWORD as environment variables
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:foodyapp02@gmail.com}
spring.mail.password=${MAIL_PASSWORD:grwwvnhchfesxsis}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# App base URL
app.base-url=${APP_BASE_URL:http://localhost:3000}
